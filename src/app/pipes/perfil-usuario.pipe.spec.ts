import { PerfilUsuarioPipe } from './perfil-usuario.pipe';

describe('PerfilUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new PerfilUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
