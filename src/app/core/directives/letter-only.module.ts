import { NgModule } from '@angular/core';
import { LetterOnlyDirective } from './letter-only.directive';

@NgModule({
  imports: [],
  declarations: [LetterOnlyDirective],
  exports: [LetterOnlyDirective],
})
export class LetterOnlyModule {}
